---
- hosts: all
  become: true
  become_user: root

  vars_files:
    - vars/main.yml

  environment:
    PYTHONHTTPSVERIFY: 0

  roles:
    - server
    - docker
    - rq1d
    - npflow

  handlers:
    - name: Apply netplan
      ansible.builtin.command: netplan apply
